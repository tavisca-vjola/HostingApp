import { RewardStats } from "./reward-stats";

export class EmployeeCard {
  id: number;
  name: string;
  tribe: string;
  squad: string;
  email: string;
  photoUrl: string;
  rewardStats: RewardStats;
}